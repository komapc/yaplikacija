{
  "name": "yaplikacija",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pronunciation trainer for hard sounds: Russian Ы [ɨ] and Hebrew/Arabic Ain [ʕ]",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "esbuild test/synth.test.ts --bundle --platform=node --format=esm --outfile=node_modules/.cache/synth.test.mjs && node node_modules/.cache/synth.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
